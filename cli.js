const axios = require("axios");

const url = "http://localhost:4001/api/blogs";
// axios.get(url).then((res) => {
//   res.data.forEach((blog) => {
//     console.log(blog.author + ":", blog.title, ",", blog.url, ",", blog.likes);
//   });
// });
